// Здесь будет возможность дернуть инит и передать ему структуру описывающую интрефейс внешнего вида устройства, с кнопками и экраном

#include "emu_graphic.h"

void emu_graphic_device_init(emu_lcd_cfg_t * cfg);
